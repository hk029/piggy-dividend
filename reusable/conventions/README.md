# conventions/

从 `CLAUDE.md` 提取的项目硬约束。复用时拷到新项目的 CLAUDE.md 里按需修改。

## 文档

| 文件 | 说明 |
|---|---|
| `hard-rules.md` | 分支管理 / 部署规范 / CSS 字号 / 图标限制 / API 响应 / 组件 memo / 订阅统一调用 / DB 不直连 / 验证方式 / 不进 git 的二进制 等 |
